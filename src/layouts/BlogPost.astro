---
import Base from "./Base.astro";
import * as styles from "./BlogPost.css.ts";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Base title={title}>
  <article class={styles.article}>
    <h1 class={styles.title}>{title}</h1>
    <slot />
  </article>
</Base>
